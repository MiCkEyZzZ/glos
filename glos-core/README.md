# glos-core — Ядро работы с IQ и метаданными

`glos-core` — это библиотечный крейт для ГЛОС, реализующий базовую работу с IQ-потоками, метаданными сигналов и сериализацией формата `.glos`.
Он служит основой для всех модулей: записи, воспроизведения, анализа и UI.

## 🎯 Цель

- Обеспечить безопасное и высокопроизводительное чтение/запись IQ-данных.
- Реализовать сериализацию и десериализацию сигналов и метаданных.
- Предоставить API для анализа, фильтрации и трансформации сигналов.

## ⚙️ Функциональность

- Работа с IQ-потоками и их метаданными.
- Сериализация/десериализация `.glos` файлов.
- Основные DSP-операции (фильтрация, преобразования).
- Асинхронная обработка потоков (`tokio` / `async-std`).
- Совместимость с внутренними инструментами ГЛОС (`glos-recorder`, `glos-replayer`, `glos-analyzer`).

## Установка

Добавьте в ваш `Cargo.toml`

```toml
[dependencies]
glos-core = "0.1"
```

## 🛠️ Технологии

- Rust — безопасное и эффективное ядро.
- `serde_cbor` / `rmp-serde` — сериализация метаданных.
- `ndarray`, `num-complex`, `rustfft` — базовая DSP-обработка.
- Поддержка интеграции с `USMET` для телеметрии и хранения.

## 🚀 Использование

В основном используется как библиотека для других крейтов ГЛОС.
Пример в коде:

```rust
use glos_core::format::GlosFile;
use glos_core::serialization::read_glos;

let data = read_glos("session1.glos")?;
println!("Файл содержит {} IQ-сэмплов", data.len());
```

## 📦 Структура

```
glos-core
├── docs
│   ├── СПЕЦИФИКАЦИЯ_ФОРМАТА.md
│   ├── план_развития.md
│   └── схема_проекта.md
├── examples
│   ├── read_glos_file.rs
│   └── write_glos_file.rs
├── src
│   ├── error.rs          # обработка ошибок
│   ├── format.rs         # форматы данных
│   ├── serialization.rs  # чтение/запись .glos
│   └── lib.rs            # публичный API
├── Cargo.toml
└── README.md
```

## Примеры

команда для запуска: `cargo run -p glos-core --example <имя файла>`

- write_glos_file.rs
- read_glos_file.rs

## 💡 Взаимодействие

- Используется всеми модулями ГЛОС: `glos-recorder`, `glos-replayer`, `glos-analyzer`.
- API позволяет расширять функциональность и интегрировать новые обработчики сигналов.
